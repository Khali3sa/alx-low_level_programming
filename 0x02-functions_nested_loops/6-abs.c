#include "main.h"
/**
 * _abs - Computes the absolute value of an integer
 * @n: The integer to be comuted
 * Return: The absolute value
 */
int _abs(int)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
